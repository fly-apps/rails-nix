<%= form_with(model: balloon) do |form| %>
  <% if balloon.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(balloon.errors.count, "error") %> prohibited this balloon from being saved:</h2>

      <ul>
        <% balloon.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
